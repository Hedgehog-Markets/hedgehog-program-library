/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import type { ResolvedAccount, ResolvedAccountsWithIndices } from "../shared";
import type { Context, Pda, PublicKey, Signer, TransactionBuilder } from "@metaplex-foundation/umi";
import type { Serializer } from "@metaplex-foundation/umi/serializers";

import { transactionBuilder } from "@metaplex-foundation/umi";
import { mapSerializer, struct, u8 } from "@metaplex-foundation/umi/serializers";

import { findLuloPoolV1Pda } from "../accounts";
import { getAccountMetasAndSigners } from "../shared";

// Accounts.
export type SetupLuloV1InstructionAccounts = {
  /** LULO pool data */
  luloPool?: PublicKey | Pda;
  /** Payer */
  payer?: Signer;
  /** System program */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type SetupLuloV1InstructionData = { discriminator: number };

export type SetupLuloV1InstructionDataArgs = {};

export function getSetupLuloV1InstructionDataSerializer(): Serializer<
  SetupLuloV1InstructionDataArgs,
  SetupLuloV1InstructionData
> {
  return mapSerializer<SetupLuloV1InstructionDataArgs, any, SetupLuloV1InstructionData>(
    struct<SetupLuloV1InstructionData>([["discriminator", u8()]], {
      description: "SetupLuloV1InstructionData",
    }),
    (value) => ({ ...value, discriminator: 11 }),
  );
}

// Instruction.
export function setupLuloV1(
  context: Pick<Context, "eddsa" | "payer" | "programs">,
  input: SetupLuloV1InstructionAccounts,
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    "hplParimutuel",
    "PARrVs6F5egaNuz8g6pKJyU4ze3eX5xGZCFb3GLiVvu",
  );

  // Accounts.
  const resolvedAccounts = {
    luloPool: {
      index: 0,
      isWritable: true as boolean,
      value: input.luloPool ?? null,
    },
    payer: {
      index: 1,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    systemProgram: {
      index: 2,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.luloPool.value) {
    resolvedAccounts.luloPool.value = findLuloPoolV1Pda(context);
  }
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111",
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: Array<ResolvedAccount> = Object.values(resolvedAccounts).sort(
    (a, b) => a.index - b.index,
  );

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(orderedAccounts, "programId", programId);

  // Data.
  const data = getSetupLuloV1InstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
