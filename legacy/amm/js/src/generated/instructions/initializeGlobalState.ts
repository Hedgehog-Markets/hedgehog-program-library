/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import type { ResolvedAccount, ResolvedAccountsWithIndices } from "../shared";
import type { Context, Pda, PublicKey, Signer, TransactionBuilder } from "@metaplex-foundation/umi";
import type { Serializer } from "@metaplex-foundation/umi/serializers";

import { transactionBuilder } from "@metaplex-foundation/umi";
import {
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u16,
  u8,
} from "@metaplex-foundation/umi/serializers";

import { findGlobalStatePda } from "../accounts";
import { getAccountMetasAndSigners } from "../shared";

// Accounts.
export type InitializeGlobalStateInstructionAccounts = {
  globalState?: PublicKey | Pda;
  upgradeAuthority: Signer;
  ammProgram: PublicKey | Pda;
  programData: PublicKey | Pda;
  payer?: Signer;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type InitializeGlobalStateInstructionData = {
  discriminator: Uint8Array;
  swapFee: number;
  feeWallet: PublicKey;
  authority: PublicKey;
  nonce: number;
};

export type InitializeGlobalStateInstructionDataArgs = {
  swapFee: number;
  feeWallet: PublicKey;
  authority: PublicKey;
  nonce: number;
};

export function getInitializeGlobalStateInstructionDataSerializer(): Serializer<
  InitializeGlobalStateInstructionDataArgs,
  InitializeGlobalStateInstructionData
> {
  return mapSerializer<
    InitializeGlobalStateInstructionDataArgs,
    any,
    InitializeGlobalStateInstructionData
  >(
    struct<InitializeGlobalStateInstructionData>(
      [
        ["discriminator", bytes({ size: 8 })],
        ["swapFee", u16()],
        ["feeWallet", publicKeySerializer()],
        ["authority", publicKeySerializer()],
        ["nonce", u8()],
      ],
      { description: "InitializeGlobalStateInstructionData" },
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([232, 254, 209, 244, 123, 89, 154, 207]),
    }),
  );
}

// Args.
export type InitializeGlobalStateInstructionArgs = InitializeGlobalStateInstructionDataArgs;

// Instruction.
export function initializeGlobalState(
  context: Pick<Context, "eddsa" | "payer" | "programs">,
  input: InitializeGlobalStateInstructionAccounts & InitializeGlobalStateInstructionArgs,
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    "hhAmm",
    "Hr4whNgXr3yZsJvx3TVSwfsFgXuSEPB1xKmvgrtLhsrM",
  );

  // Accounts.
  const resolvedAccounts = {
    globalState: {
      index: 0,
      isWritable: true as boolean,
      value: input.globalState ?? null,
    },
    upgradeAuthority: {
      index: 1,
      isWritable: false as boolean,
      value: input.upgradeAuthority ?? null,
    },
    ammProgram: {
      index: 2,
      isWritable: false as boolean,
      value: input.ammProgram ?? null,
    },
    programData: {
      index: 3,
      isWritable: false as boolean,
      value: input.programData ?? null,
    },
    payer: {
      index: 4,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    systemProgram: {
      index: 5,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitializeGlobalStateInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.globalState.value) {
    resolvedAccounts.globalState.value = findGlobalStatePda(context);
  }
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111",
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: Array<ResolvedAccount> = Object.values(resolvedAccounts).sort(
    (a, b) => a.index - b.index,
  );

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(orderedAccounts, "programId", programId);

  // Data.
  const data = getInitializeGlobalStateInstructionDataSerializer().serialize(resolvedArgs);

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
