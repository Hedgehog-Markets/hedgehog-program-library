/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import type { ResolvedAccount, ResolvedAccountsWithIndices } from "../shared";
import type { Context, Pda, PublicKey, Signer, TransactionBuilder } from "@metaplex-foundation/umi";
import type { Serializer } from "@metaplex-foundation/umi/serializers";

import { transactionBuilder } from "@metaplex-foundation/umi";
import { bytes, mapSerializer, struct, u8 } from "@metaplex-foundation/umi/serializers";

import { findCreatorMetadataPda } from "../accounts";
import { expectPublicKey, getAccountMetasAndSigners } from "../shared";

// Accounts.
export type InitializeCreatorMetadataInstructionAccounts = {
  creatorMetadata?: PublicKey | Pda;
  creator: Signer;
  payer?: Signer;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type InitializeCreatorMetadataInstructionData = {
  discriminator: Uint8Array;
  nonce: number;
};

export type InitializeCreatorMetadataInstructionDataArgs = { nonce: number };

export function getInitializeCreatorMetadataInstructionDataSerializer(): Serializer<
  InitializeCreatorMetadataInstructionDataArgs,
  InitializeCreatorMetadataInstructionData
> {
  return mapSerializer<
    InitializeCreatorMetadataInstructionDataArgs,
    any,
    InitializeCreatorMetadataInstructionData
  >(
    struct<InitializeCreatorMetadataInstructionData>(
      [
        ["discriminator", bytes({ size: 8 })],
        ["nonce", u8()],
      ],
      { description: "InitializeCreatorMetadataInstructionData" },
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([246, 1, 31, 127, 199, 240, 100, 77]),
    }),
  );
}

// Args.
export type InitializeCreatorMetadataInstructionArgs = InitializeCreatorMetadataInstructionDataArgs;

// Instruction.
export function initializeCreatorMetadata(
  context: Pick<Context, "eddsa" | "payer" | "programs">,
  input: InitializeCreatorMetadataInstructionAccounts & InitializeCreatorMetadataInstructionArgs,
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    "hhAmm",
    "Hr4whNgXr3yZsJvx3TVSwfsFgXuSEPB1xKmvgrtLhsrM",
  );

  // Accounts.
  const resolvedAccounts = {
    creatorMetadata: {
      index: 0,
      isWritable: true as boolean,
      value: input.creatorMetadata ?? null,
    },
    creator: {
      index: 1,
      isWritable: false as boolean,
      value: input.creator ?? null,
    },
    payer: {
      index: 2,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    systemProgram: {
      index: 3,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitializeCreatorMetadataInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.creatorMetadata.value) {
    resolvedAccounts.creatorMetadata.value = findCreatorMetadataPda(context, {
      creator: expectPublicKey(resolvedAccounts.creator.value),
    });
  }
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111",
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: Array<ResolvedAccount> = Object.values(resolvedAccounts).sort(
    (a, b) => a.index - b.index,
  );

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(orderedAccounts, "programId", programId);

  // Data.
  const data = getInitializeCreatorMetadataInstructionDataSerializer().serialize(resolvedArgs);

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
