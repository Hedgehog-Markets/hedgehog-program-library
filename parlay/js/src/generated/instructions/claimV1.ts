/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import type { ResolvedAccount, ResolvedAccountsWithIndices } from "../shared";
import type { Context, Pda, PublicKey, Signer, TransactionBuilder } from "@metaplex-foundation/umi";
import type { Serializer } from "@metaplex-foundation/umi/serializers";

import { findAssociatedTokenPda } from "@metaplex-foundation/mpl-toolbox";
import { transactionBuilder } from "@metaplex-foundation/umi";
import { mapSerializer, struct, u8 } from "@metaplex-foundation/umi/serializers";

import { findDepositPda, findPlatformFeesPda } from "../../hooked";
import { expectPublicKey, getAccountMetasAndSigners } from "../shared";

// Accounts.
export interface ClaimV1InstructionAccounts {
  /** Market */
  market: PublicKey | Pda;
  /** Market entry */
  entry: PublicKey | Pda;
  /** Deposit token mint */
  mint: PublicKey | Pda;
  /** Deposit token account */
  deposit?: PublicKey | Pda;
  /** User token account */
  tokenAccount?: PublicKey | Pda;
  /** Creator fees account */
  creatorFees: PublicKey | Pda;
  /** Platform fees account */
  platformFees?: PublicKey | Pda;
  /** User wallet */
  wallet?: Signer;
  /** SPL token program */
  tokenProgram?: PublicKey | Pda;
}

// Data.
export interface ClaimV1InstructionData {
  discriminator: number;
}

export interface ClaimV1InstructionDataArgs {}

export function getClaimV1InstructionDataSerializer(): Serializer<
  ClaimV1InstructionDataArgs,
  ClaimV1InstructionData
> {
  return mapSerializer<ClaimV1InstructionDataArgs, any, ClaimV1InstructionData>(
    struct<ClaimV1InstructionData>([["discriminator", u8()]], {
      description: "ClaimV1InstructionData",
    }),
    (value) => ({ ...value, discriminator: 5 }),
  );
}

// Instruction.
export function claimV1(
  context: Pick<Context, "eddsa" | "identity" | "programs">,
  input: ClaimV1InstructionAccounts,
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    "hplParlay",
    "PLYaNRbQs9GWyVQdcLrzPvvZu7NH4W2sneyHcEimLr7",
  );

  // Accounts.
  const resolvedAccounts = {
    market: {
      index: 0,
      isWritable: false as boolean,
      value: input.market ?? null,
    },
    entry: {
      index: 1,
      isWritable: true as boolean,
      value: input.entry ?? null,
    },
    mint: { index: 2, isWritable: false as boolean, value: input.mint ?? null },
    deposit: {
      index: 3,
      isWritable: true as boolean,
      value: input.deposit ?? null,
    },
    tokenAccount: {
      index: 4,
      isWritable: true as boolean,
      value: input.tokenAccount ?? null,
    },
    creatorFees: {
      index: 5,
      isWritable: true as boolean,
      value: input.creatorFees ?? null,
    },
    platformFees: {
      index: 6,
      isWritable: true as boolean,
      value: input.platformFees ?? null,
    },
    wallet: {
      index: 7,
      isWritable: false as boolean,
      value: input.wallet ?? null,
    },
    tokenProgram: {
      index: 8,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.deposit.value) {
    resolvedAccounts.deposit.value = findDepositPda(context, {
      market: expectPublicKey(resolvedAccounts.market.value),
    });
  }
  if (!resolvedAccounts.wallet.value) {
    resolvedAccounts.wallet.value = context.identity;
  }
  if (!resolvedAccounts.tokenAccount.value) {
    resolvedAccounts.tokenAccount.value = findAssociatedTokenPda(context, {
      mint: expectPublicKey(resolvedAccounts.mint.value),
      owner: expectPublicKey(resolvedAccounts.wallet.value),
    });
  }
  if (!resolvedAccounts.platformFees.value) {
    resolvedAccounts.platformFees.value = findPlatformFeesPda(context, {
      mint: expectPublicKey(resolvedAccounts.mint.value),
    });
  }
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = context.programs.getPublicKey(
      "splToken",
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
    );
    resolvedAccounts.tokenProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: Array<ResolvedAccount> = Object.values(resolvedAccounts).sort(
    (a, b) => a.index - b.index,
  );

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(orderedAccounts, "programId", programId);

  // Data.
  const data = getClaimV1InstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
